openapi: 3.0.3
info:
  title: School Manager SaaS API
  description: Enterprise-grade Multi-Tenant School Management Platform (ERP) API. This API provides access to global administration, pedagogical modules, financial systems, and public lead capture endpoints.
  version: 1.0.0
  contact:
    name: SGE Development Team
servers:
  - url: http://localhost:8000/api
    description: Local Development Server

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    PreEnrollment:
      type: object
      properties:
        id:
          type: integer
          example: 1
        parent_name:
          type: string
          example: "João Silva"
        phone:
          type: string
          example: "+55 11 99999-9999"
        email:
          type: string
          format: email
          example: "joao@example.com"
        student_name:
          type: string
          example: "Pedrinho Silva"
        grade_id:
          type: integer
          description: ID da série pretendida.
          example: 2
        status:
          type: string
          enum: [pending, approved, rejected]
          example: "pending"

    Student:
      type: object
      properties:
        id:
          type: integer
          example: 101
        unit_id:
          type: integer
          example: 1
        name:
          type: string
          example: "Carlos Pereira"
        birth_date:
          type: string
          format: date
          example: "2010-05-15"
        status:
          type: string
          example: "active"

security:
  - bearerAuth: []

paths:
  /lead/store:
    post:
      summary: Enviar solicitação de Pré-Matrícula (Público)
      description: Endpoint utilizado pela Landing Page pública para captar intenções de matrículas de novos alunos.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - parent_name
                - phone
                - student_name
                - grade_id
              properties:
                parent_name:
                  type: string
                phone:
                  type: string
                email:
                  type: string
                  format: email
                student_name:
                  type: string
                grade_id:
                  type: integer
      responses:
        '201':
          description: Lead criado com sucesso
        '422':
          description: Erro de Validação

  /secretariat/leads:
    get:
      summary: Listar Leads Pendentes
      description: Retorna a lista de intenções de matrículas que aguardam aprovação da secretaria. Requer role `secretaria` ou `admin`.
      responses:
        '200':
          description: Sucesso
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PreEnrollment'

  /secretariat/leads/{lead_id}/approve:
    post:
      summary: Aprovar Lead (Converter em Aluno)
      description: Pega os dados do Lead e insere automaticamente na tabela de Estudantes oficiais, marcando o lead como `approved`.
      parameters:
        - in: path
          name: lead_id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Lead convertido com sucesso

  /academic/smart-tools/auto-enroll:
    post:
      summary: Enturmação em Lote (Assistente IA)
      description: Distribui matematicamente um array de estudantes entre as turmas de uma determinada Série (Grade), respeitando o limite de capacidade de cada turma e garantindo homogeneidade.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - student_ids
                - grade_id
              properties:
                student_ids:
                  type: array
                  items:
                    type: integer
                grade_id:
                  type: integer
      responses:
        '200':
          description: Estudantes enturmados e balanceados
        '422':
          description: Falha de validação ou turmas inexistentes

  /admin/official-documents:
    get:
      summary: Listar Documentos Oficiais (Ofícios, Portarias)
      description: Retorna a listagem paginada de todos os expedientes oficiais da Unidade ativa.
      responses:
        '200':
          description: Listagem de Documentos
    post:
      summary: Emitir Documento Oficial (Rascunho)
      description: Inicia um novo Ofício com numeração automática sequencial segura (lockForUpdate).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                category_id:
                  type: integer
                date:
                  type: string
                  format: date
                recipient:
                  type: string
                subject:
                  type: string
                content:
                  type: string
                signer_id:
                  type: integer
      responses:
        '201':
          description: Ofício Rascunho criado com sucesso

  /admin/official-documents/{id}/pdf:
    get:
      summary: Gerar PDF ABNT do Documento
      description: Baixa o PDF formatado do ofício com brasão, rodapé oficial e métricas da ABNT.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: PDF Gerado com sucesso
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  /secretariat/protocols:
    get:
      summary: Listar Protocolos Recebidos
      description: Listagem de documentos que entraram na Unidade (Inbound), com avisos de SLA.
      responses:
        '200':
          description: Sucesso
    post:
      summary: Registrar Recebimento de Protocolo
      description: Cria um novo registro e aceita arrays de arquivos como anexos (multipart/form-data).
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                sender:
                  type: string
                subject:
                  type: string
                received_date:
                  type: string
                  format: date
                due_date:
                  type: string
                  format: date
                attachments:
                  type: array
                  items:
                    type: string
                    format: binary
      responses:
        '201':
          description: Protocolo criado
